# 算法指南

本文档详细介绍 ephemeris 项目使用的天文算法原理、实现细节和精度分析。

---

## 目录

1. [天文算法原理](#天文算法原理)
2. [算法实现](#算法实现)
3. [精度与校准](#精度与校准)
4. [改进尝试记录](#改进尝试记录)

---

## 天文算法原理

### 物理黄经 vs 视黄经

在 Swiss Ephemeris 项目中，计算天体的视黄经（Apparent Ecliptic Longitude）是一个多步骤的精密矫正过程。

#### 物理黄经（Geometric Longitude）

**物理黄经**（或称几何位置）是所有修正前的起始数据，基于历表文件直接计算：

1. **高精度模式：JPL 历表 (DE431/DE441)**
   - 历表文件存储切比雪夫多项式系数
   - 通过插值计算 J2000 平赤道笛卡尔坐标
   - 精度：米级

2. **标准模式：Swiss Ephemeris 历表 (.se1 文件)**
   - 压缩的 JPL 历表版本
   - 提供更长年限支持
   - 精度：约 0.1°

3. **快速模式：Moshier 模型**
   - 半分析模型（基于 ELP2000 和 VSOP87）
   - 无需外部文件
   - 精度：约 1-2°

#### 坐标转换

无论通过哪种历表，最初得到的通常是**赤道坐标 (J2000 Equator)**。为了得到"物理黄经"，需要进行坐标旋转：

1. **旋转**：使用 J2000 历元的标准黄赤交角（约 23.439°）
2. **转换**：将旋转后的笛卡尔坐标转换为极坐标
3. **结果**：此时的经度分量即为**物理黄经 (Geometric Longitude, J2000)**

### 视黄经修正流程

从物理黄经矫正为视黄经的关键步骤：

1. **光时修正 (Light-time Correction)**
   - 迭代求解光行时
   - 收敛精度：约 5 米

2. **引力偏折 (Gravitational Deflection)**
   - 广义相对论效应
   - 太阳边缘约 1.75 角秒

3. **光行差 (Annual Aberration)**
   - 地球公转导致的光线偏移
   - 约 20.5 角秒

4. **岁差 (Precession)**
   - 地球自转轴长期进动
   - 约 50.3 角秒/年

5. **章动 (Nutation)**
   - 地球自转轴的周期性摆动
   - 约 9-17 角秒

---

## 算法实现

### 算法版本对比

| 算法 | 完整版本 | 简化版本 | 当前使用 |
|------|---------|---------|---------|
| **章动 (Nutation)** | IAU 2000A (678 项) | IAU 2000B (77 项) | ✅ IAU 2000A |
| **岁差 (Precession)** | IAU 2006 / Vondrák 2011 | 无 | ✅ Vondrák 2011 |
| **光行差 (Aberration)** | 相对论完整公式 | 简化公式 (K ≈ 20.5") | ✅ 完整公式 |
| **引力偏折** | 广义相对论完整公式 | 无 | ✅ 完整公式 |
| **光时修正** | 迭代求解 | 单次近似 | ✅ 迭代求解 |

### 1. 章动 (Nutation)

**完整版本：IAU 2000A** ✅

```rust
// nutation_iau2000a_impl.rs
pub fn nutation_iau2000a(jd: f64) -> (f64, f64) {
    // 678 项级数展开
    // 精度：约 0.001 角秒
}
```

**简化版本：IAU 2000B**
```rust
pub fn nutation_iau2000b(jd: f64) -> (f64, f64) {
    // 77 项级数展开
    // 精度：约 0.1 角秒
}
```

**当前使用**：完整版本 IAU 2000A

### 2. 岁差 (Precession)

**默认模型：Vondrák 2011** ✅

```rust
// corrections.rs
pub fn precession_vondrak_2011(t: f64) -> (f64, f64, f64) {
    // 22 个周期项 + 8 个多项式项
    // 适用于 -6000 年 ~ +6000 年
}
```

**短时期模型：IAU 2006**
```rust
pub fn precession_iau2006(t: f64) -> (f64, f64, f64) {
    // IAU 2006 岁差公式
    // 适用于 1900 年 ~ 2100 年
}
```

**模型对比：**

| 模型 | 适用范围 | 周期项 | 多项式项 |
|------|---------|--------|---------|
| Vondrák 2011 | -6000~+6000 年 | 22 项 | 8 项 |
| IAU 2006 | 1900~2100 年 | 0 项 | 5 项 |

### 3. 光行差 (Annual Aberration)

**完整版本：相对论公式** ✅

```rust
// corrections.rs
pub fn annual_aberration(
    body_pos: Cartesian,
    earth_vel: Cartesian,
) -> Cartesian {
    // 相对论光行差公式
    // u' = (u / gamma + v/c * (1 + u·v/c / (1 + 1/gamma))) / (1 + u·v/c)
    // 精度：最高
}
```

**简化版本** ⚠️
```rust
pub fn annual_aberration_approx(
    body_pos: Spherical,
    earth_vel: Spherical,
) -> Spherical {
    // 简化公式：Δλ = -K * cos(λ - λ_sun) / cos(β)
    // K ≈ 20.4955 角秒
    // 精度：约 0.1 角秒
}
```

**当前使用**：完整版本

### 4. 岁差修正说明

**问题发现**：IAU 2006 岁差公式的霍纳法则系数顺序错误导致计算结果不准确。

**修复前**（错误）：
```rust
let zeta_arcsec = 2.650545 + 2306.083227 * t + 0.2988499 * t2 + ...
```

**修复后**（正确）：
```rust
let zeta_arcsec = (((((-0.0000003173 * t - 0.000005971) * t + 0.01801828) * t + 0.2988499) * t + 2306.083227) * t + 2.650545) * t;
```

**参考**：Swiss Ephemeris `swephlib.c` `precess_1()` `SEMOD_PREC_IAU_2006`

---

## 精度与校准

### 朔望和节气计算校准

#### 算法精度对比

| 算法 | 精度 | 速度 | 依赖 |
|------|------|------|------|
| 近似算法 | 约 1 秒 | 快 | 无 |
| anise + JPL DE440 | <0.01 秒 | 中等 | BSP 历表文件 |
| Swiss Ephemeris | <0.1 秒 | 快 | 历表文件 |

#### 误差来源分析

1. **数值迭代次数限制**
   - 使用 3 次牛顿迭代
   - 增加迭代次数可提高精度但降低性能

2. **ΔT 计算**
   - 力学时 (TT) 与世界时 (UT1) 的转换
   - 使用近似多项式而非实测数据

3. **浮点数精度累积**
   - 多次三角函数计算的舍入误差
   - f64 精度约 15-17 位有效数字

#### 测试数据校准

**so_high（朔时刻计算）**
```
输入：w = 1727.8759594743863 rad
原始参考值：8126.101574259753
当前算法输出：8126.101589377018
误差：1.3061 秒
```

**so_low（朔时刻计算 - 历史日期）**
```
输入：w = -4354.247417875453 rad
原始参考值：-20458.974805811675
当前算法输出：-20458.974805811675
误差：0 秒（恰好一致）
```

**qi_hight（节气时刻计算）**
```
输入：w = 58.119464091411174 rad
原始参考值：3093.8331491526683
当前算法输出：3093.8331506680383
误差：0.1309 秒
```

### 历史年份测试数据

测试覆盖范围：公元前 3000 年 - 公元 3000 年

**测试点：**
- 10 个历史年份的朔望时刻
- 所有测试点误差 < 0.1 秒
- 算法稳定性良好

### so_high 误差分析

**误差范围**：约 1-2 秒

**原因**：
1. 使用 `moon_a_lon_t2` 快速近似
2. 3 次牛顿迭代收敛
3. ΔT 近似公式

**改进建议**：
- 增加迭代次数到 5 次
- 使用更精确的 ΔT 模型
- 或切换到 JPL 历表

---

## 改进尝试记录

### 已尝试的改进

1. **Vondrák 2011 岁差模型** ✅
   - 适用于长时期计算
   - 包含 22 个周期项
   - 精度优于 IAU 2006

2. **完整 IAU 2000A 章动** ✅
   - 678 项级数展开
   - 精度约 0.001 角秒

3. **anise 库光行差修正** ✅
   - 使用 `Aberration::CN_S`
   - 收敛光时 + 恒星
   - 精度约 0.001 角秒

### 待解决的问 题

**JPL 历表坐标系差异**

当前 JPL DE441 计算的太阳位置与 Swiss Ephemeris 差异约 166°：
- JPL DE441: -78.72° (即 281.28°)
- Swiss Eph: 87.90°

**可能原因**：
1. 岁差修正缺失（J2000 → 观测时刻）
2. 坐标系理解错误
3. 光行差方向问题

**下一步行动**：
1. 检查 anise 文档确认坐标系
2. 添加岁差修正
3. 对比 JPL Horizons 在线星历

---

## 参考资料

1. **Capitaine et al. (2003)** - IAU 2006 岁差模型原始文献
2. **Swiss Ephemeris** - `swephlib.c` `precess_1()`
3. **anise 库文档** - https://docs.rs/anise/
4. **JPL Horizons** - https://ssd.jpl.nasa.gov/horizons/

---

## 更新记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-03-29 | 1.0 | 整合算法说明、岁差修正、校准文档 |
