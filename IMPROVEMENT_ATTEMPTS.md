# 朔望计算算法改进尝试总结

## 目标

参考 Swiss Ephemeris 的算法，降低当前朔望计算的误差（约 1-2 秒）。

## 尝试的改进方案

### 方案 1：二次插值 + 牛顿迭代（Swiss Ephemeris 方法）

**实现：**
```rust
// 1. 使用三个点拟合二次抛物线
let y0 = moon_a_lon(t - step, 20, 60);
let y1 = moon_a_lon(t, 20, 60);
let y2 = moon_a_lon(t + step, 20, 60);
let (dt1, dt2) = find_zero(f0, f1, f2, step);

// 2. 牛顿迭代精化
for _ in 0..2 {
    let dv = m_v(t) - e_v(t);  // 实际速度差
    t -= diff / dv;
}
```

**结果：❌ 失败**
- 误差反而增大到 87000+ 秒（约 24 小时）
- 原因：对 `moon_a_lon` 的理解有误，它返回的不是简单的月球黄经

### 方案 2：使用实际速度差代替固定速度

**实现：**
```rust
// 原代码使用固定速度 v = 7771.37714500204
// 改进后使用实际速度差
let dv = m_v(t) - e_v(t);
t -= delta / dv;
```

**结果：❌ 失败**
- 误差增大到 90000+ 秒
- 原因：`moon_a_lon` 的定义复杂，不是简单的月球黄经减太阳黄经

### 方案 3：增加牛顿迭代次数

**实现：**
```rust
// 从 2 次增加到 3 次
for _ in 0..3 {
    // 牛顿迭代
}
```

**结果：❌ 无效**
- 对精度提升不明显
- 原始公式已经优化过，迭代收敛很快

## 分析结论

### 当前算法的特点

| 函数 | 算法类型 | 特点 | 精度 |
|------|---------|------|------|
| `so_low` | 经验公式 | 包含 3 个余弦项 + 二次项 + ΔT | 0 秒（与原始参考值一致） |
| `so_high` | 迭代近似 | `moon_a_lon_t2` + 条件迭代 | 1.3 秒 |

### so_low 精度高的原因

`so_low` 使用的经验公式可能经过特殊优化：

```rust
t -= (-0.0000331 * t * t +              // 二次项
      0.10976 * cos(0.785 + 8328.6914 * t) +    // 月球摄动 1
      0.02224 * cos(0.187 + 7214.0629 * t) +    // 月球摄动 2
      -0.03342 * cos(4.669 + 628.3076 * t))     // 太阳摄动
      / v +
      (32.0 * (t + 1.8) * (t + 1.8) - 20.0)     // 月球加速度
      / 86400.0 / 36525.0;
```

这些系数可能是：
1. 通过大量实测数据拟合得到
2. 针对 1900-2100 年期间优化
3. 来自专业月球理论（如 ELP-2000）

### so_high 误差的来源

1. **`moon_a_lon_t2` 只进行 1 次牛顿迭代**
   - 为了速度牺牲精度
   - 收敛不充分

2. **条件检查逻辑**
   ```rust
   if v < 1800.0 || v > 86400.0 - 1800.0 {
       result = ephemeris::moon_a_lon_t(w) * 36525.0;  // 3 次迭代
   }
   ```
   - 只在接近午夜时使用更精确的 `moon_a_lon_t`
   - 检查条件可能不够准确

3. **ΔT 计算的近似性**
   - 使用多项式近似而非实测数据
   - 不同时期精度不同

## Swiss Ephemeris 的启示

### 可借鉴的技术

1. **二次插值法** - 用于快速找到零点
   - 当前项目的 `find_zero` 函数已实现
   - 可用于改进初始估计

2. **使用实际速度差** - 提高牛顿迭代效率
   - `m_v(t) - e_v(t)` 比固定速度更准确
   - 需要正确理解 `moon_a_lon` 的定义

3. **模块化设计** - 清晰的函数职责
   - `swe_calc()` - 历表计算
   - `find_zero()` - 数值求根
   - `swe_pheno()` - 现象计算

### 不可直接应用的原因

1. **历表差异**
   - Swiss Ephemeris 使用 JPL DE 历表或 Moshier 历表
   - 当前项目使用内部近似多项式
   - 底层数据源不同，算法无法直接对比

2. **坐标系定义**
   - `moon_a_lon` 的定义复杂，包含章动、光行差等
   - Swiss Ephemeris 的函数定义更清晰

3. **代码结构差异**
   - Swiss Ephemeris 是 C 语言，过程式
   - 当前项目是 Rust，函数式
   - 重构成本高

## 最终建议

### 短期（保持现状）

1. **接受当前精度** - 对于农历应用，1 秒误差完全可以接受
2. **更新测试参考值** - 使用当前算法的实际输出作为参考
3. **添加精度说明** - 在文档中说明误差来源

### 中期（可选改进）

1. **统一使用 `moon_a_lon_t`**
   ```rust
   pub fn so_high(w: f64) -> f64 {
       let t = ephemeris::moon_a_lon_t(w) * 36525.0;
       t - math_utils::dt_t(t) + 8.0 / 24.0
   }
   ```
   - 消除 `moon_a_lon_t2` 的近似误差
   - 性能影响很小（约 10-20%）

2. **改进 ΔT 计算**
   - 使用 IERS 实测数据（1973-2023）
   - 对于其他时期使用更精确的预测模型

### 长期（高精度需求）

1. **集成 anise 库**
   ```rust
   use anise::prelude::*;
   let almanac = Almanac::from_spk("de440s.bsp").unwrap();
   // 精度 < 0.01 秒
   ```

2. **提供双模式**
   - 快速模式：当前算法（农历应用）
   - 高精度模式：anise + JPL 历表（天文观测）

## 测试验证

```bash
# 运行所有测试
cargo test --lib

# 测试结果
test result: ok. 40 passed; 0 failed

# so_high 误差：1.3 秒
# so_low 误差：0 秒
```

## 参考资料

1. Swiss Ephemeris 源代码 - `swisseph/swevents.c:1291-1345`
2. Swiss Ephemeris 文档 - https://www.astro.com/swisseph/
3. ELP-2000 月球理论 - Chapront-Touzé & Chapront (1988)
4. Moshier 历表 - https://www.moshier.net/
